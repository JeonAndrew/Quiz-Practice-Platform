#include <iostream>

#include "Course.h"
#include "Topic.h"

int main() {
    std::cout << "Hello, WorldSSSSS!" << std::endl;
    Course NewCourse = Course("CS101");
    return 0;

    // Once User logs in, generate the User object that corresponds with them
    // Use userID as a means to retrieve specific data regarding topic proficiencies in the DB
    // Generate Courses
    // Generate Topics for each course
    // Populate the vector of Questions within each Topic

}
